const fs = require("fs");
const path = require("path");

const chunks = [
  [1,10],[11,20],[21,30],[31,40]
];

// Paste merged in extractQuestions - we'll write from user message via separate data file
console.log("Use data/building-materials.json directly");
