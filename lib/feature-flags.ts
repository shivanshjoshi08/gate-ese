/** Learner UI: PYQ practice bank + PYQ progress cards (re-enable when ready). */
export const USER_PYQ_ENABLED = false;

/** ESE/GATE previous-year PDF downloads (`/pyq-pdfs`) — independent of PYQ practice bank. */
export const USER_PYQ_PDFS_ENABLED = true;
